version: '3'

services:
  db:
    image: postgres:10.5-alpine
    env_file:
      - .env
    ports:
      - "${POSTGRES_EXTERNAL_PORT}:5432"
    volumes:
      - "../volumes/pgdata:/var/lib/postgresql/data"
  bolsistas:
    image: 'kelsoncm/django_psycopg2:1.0'
    build:
      context: ../dockers/app
    ports:
      - "${APP_EXTERNAL_PORT}:8000"
    env_file:
      - .env
    volumes:
      - '../apps:/apps'
    depends_on:
      - db
